- name: Consultar versão do IOS
  hosts: all
  gather_facts: no

  tasks:
    - name: Executar show version
      command: show version
      register: output

    - debug:
        var: output.stdout
